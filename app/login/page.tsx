'use client'
import {FormEvent,useState} from 'react'
import {LockKeyhole,Phone,PackageCheck,Eye,EyeOff,ShieldCheck} from 'lucide-react'
export default function Login(){
 const [phone,setPhone]=useState(''),[password,setPassword]=useState(''),[show,setShow]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState('')
 async function submit(e:FormEvent){e.preventDefault();setError('');setBusy(true);try{const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({phone,password})});const d=await r.json();if(!r.ok){setError(d.error||'شماره موبایل یا رمز عبور اشتباه است');return}location.href='/'}catch{setError('ارتباط با سرور برقرار نشد')}finally{setBusy(false)}}
 return <div className="login-page"><div className="login-card"><div className="login-logo"><PackageCheck size={30}/></div><div className="eyebrow" style={{justifyContent:'center'}}><ShieldCheck size={14}/> ورود امن</div><h1>ورود به Cargo Manager</h1><p className="muted">با شماره موبایل و رمز عبور وارد حساب خود شوید.</p>{error&&<div className="error" style={{marginTop:16}}>{error}</div>}<form onSubmit={submit} style={{marginTop:22}}><label className="muted">شماره موبایل</label><div className="login-input"><Phone size={18}/><input dir="ltr" autoFocus inputMode="numeric" placeholder="09xxxxxxxxx" value={phone} onChange={e=>setPhone(e.target.value)}/></div><label className="muted">رمز عبور</label><div className="login-input"><LockKeyhole size={18}/><input dir="ltr" type={show?'text':'password'} placeholder="رمز عبور" value={password} onChange={e=>setPassword(e.target.value)}/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div><button className="btn big login-btn" disabled={busy||!phone||!password}>{busy?'در حال ورود...':'ورود به سامانه'}</button></form><div className="login-note">حساب ادمین و حساب راکب‌ها سطح دسترسی جداگانه دارند.</div></div></div>
}
