# Prisma CLI only (legacy/local SQLite tooling). The deployed Worker uses the DB binding from wrangler.jsonc.
DATABASE_URL="file:./cargo.db"

# Set production values using: npx wrangler secret put <NAME>
AUTH_SECRET="replace-with-a-long-random-secret"
NEDEX_WS_TOKEN="replace-with-nedex-token"
